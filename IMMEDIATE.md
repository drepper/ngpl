[x] keep a central list of imported files with names and identifying data (device, inode).  Before parsing
    a found file check the cache first and reuse the cached content if found.
[x] the definition of structs also can use the form with { }.  Implement and document this
