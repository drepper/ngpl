// The second file names Level, which the first declared, so it says so.
// It also calls shout(), which the third defines: a function may be
// written below whatever calls it, so nothing has to be said about
// that.

@import("./a.ngpl")

fn announce(l : Level) → str:
    greeting(l) ⧺ ", " ⧺ shout(l)
