// A name beginning with a slash says where the file is, so nothing is
// searched: it is there or it is not.
@import("/no/such/file/anywhere.ngpl")

@start
@impure
fn main():
    std.println("never")
