// The first thing a recipe is handed is the build it declares on, and
// a recipe that takes something else has nothing to declare it on.
@build
fn build(n : i64):
    _ ← n

@start
fn main():
    _ ← 1
