// Fixture for the REPL preload tests: definitions the session inherits.

fn triple(n : i64) → i64:
    n × 3

let LIMIT : i64 = 99

@start
@impure
fn main():
    std.println("main should not run under --repl")
