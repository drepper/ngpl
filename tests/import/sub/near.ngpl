// Bound from tests/import/deeper.ngpl, which is a directory away from
// the file the module is rooted in: what a name is looked for beside
// is the file that asked for it.
@export
fn five() → i64:
    5
