// Bound from tests/import/lib.ngpl, which is itself bound from a
// directory away: what a name is looked for beside is the file that
// asked for it, and never the one that asked for that.
@export
fn five() → i64:
    5
