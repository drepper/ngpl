// A literal too large for its type would become an infinity,
// which is a different number from the one written.  The suffix says f64, and f64 does not go that high.
@start
@impure
fn main():
    let a := 3e400f64
    std.println("{}", a)
