// An arbitrary-precision field has no C type to match it to.  In the
// bootstrap it does not get that far: the type itself is not provided,
// and the error names the field and the sized type to use instead.

@repr(C)
struct Header:
    magic : u32
    length : int

@start
fn main() → ∅:
    std.print(Header.sizeof)
