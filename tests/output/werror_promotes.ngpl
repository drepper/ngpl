// -Werror makes a warning an error, and an error stops the run: the
// startup function below never runs, so nothing is printed.
@start
@impure
fn main():
    let n : mut i64 = 1
    std.println("{}", n)
    std.println("main ran")
