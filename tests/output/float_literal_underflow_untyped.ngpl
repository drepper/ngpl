// A literal too small for its type would become a zero, which is
// a different number from the one written.  No suffix, so the bootstrap holds it in an f64, which is the
// range it has until the arbitrary-precision float arrives.
@start
@impure
fn main():
    let a := 1e-400
    std.println("{}", a)
