// Two widths are two types.  Where a sum type says they belong
// together this is one value; nothing here says so.
@start
@impure
fn main():
    let a : i32 = 1
    let b : i64 = 2
    let chosen := if true: a else: b
    std.println("{}", chosen)
