// A tuple settles nothing between its elements: one of them stating a
// width says nothing about the others.  Either the binding states the
// tuple's type, which the diagnostic writes out, or every number
// states its own.
@start
@impure
fn main():
    let t := (1, "two")
    std.println("{}", t[0])
