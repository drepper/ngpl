error: in main: a lambda's return type is bool, which is not an array type, but the body hands back an array: an operator handed one answers one for each of what it holds, so this is 'bool[]'
  --> tests/output/threaded_return_type_lambda_inside.ngpl:5:33
    |
  5 |     let g := λy : i32[] → bool: y > 3
    |                                 ^^^^^
  6 |     std.println("not reached")
    |
